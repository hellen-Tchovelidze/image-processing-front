{
  "version": 3,
  "build": {
    "env": {
      "NEXT_PUBLIC_API_URL": "localhost:3001",
      "AWS_BUCKET_NAME": "@aws_bucket_name"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/$1" }
  ]
}
