/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    experimental: {
      serverActions: true, // optional, თუ იყენებ
    },
  };
  
  module.exports = nextConfig;
  